use vortex::file::{VortexFileArrayStream, VortexReadBuilder};

// We want to have fine-grained control over the runtime when we record our measurements here.
fn main() {
    VortexReadBuilder::new()
}
